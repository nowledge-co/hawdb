[package]
name = "skein-relational"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
skein-core = { path = "../core" }
skein-sql = { path = "../sql" }
skein-storage = { path = "../storage" }
