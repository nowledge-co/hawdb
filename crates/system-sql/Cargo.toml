[package]
name = "hawdb-system-sql"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde_json = "1"
hawdb-core = { path = "../core" }
hawdb-executor = { path = "../executor" }
hawdb-plan-cache = { path = "../plan-cache" }
hawdb-query = { path = "../query" }
hawdb-sql = { path = "../sql" }
hawdb-storage = { path = "../storage" }

[dev-dependencies]
loom = "0.7"

[features]
default = []
loom-tests = []
