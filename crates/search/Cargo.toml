[package]
name = "hawdb-search"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
bitpacking = { version = "=0.9.3", default-features = false, features = ["bitpacker4x"] }
fst = "=0.4.7"
chrono = { version = "0.4", default-features = false, features = ["std"] }
# Opaque analyzer capacity bounds are qualified against this dependency set.
jieba-rs = "=0.10.3"
regex = { version = "=1.13.1", default-features = false }
regex-automata = { version = "=0.4.16", default-features = false }
regex-syntax = { version = "=0.8.11", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
simsimd = { version = "6.5", optional = true }
zstd = "0.13"
hawdb-core = { path = "../core", version = "0.2.0" }
hawdb-evidence = { path = "../evidence", version = "0.2.0" }
hawdb-executor = { path = "../executor", version = "0.2.0" }
hawdb-integrity = { path = "../integrity", version = "0.2.0" }
hawdb-optimizer = { path = "../optimizer", version = "0.2.0" }
hawdb-plan = { path = "../plan", version = "0.2.0" }
hawdb-qos = { path = "../qos", version = "0.2.0" }
hawdb-storage = { path = "../storage", version = "0.2.0" }
hawdb-telemetry = { path = "../telemetry", version = "0.2.0" }
hawdb-vector-projection = { path = "../vector-projection", version = "0.2.0", optional = true }

[features]
default = [
    "background-maintenance",
    "full-text-search",
    "graph-analytics",
    "vector-search",
]
acl = []
background-maintenance = []
full-text-search = []
graph-analytics = []
qualification = []
vector-search = ["dep:simsimd", "dep:hawdb-vector-projection"]

[dev-dependencies]
hawdb-vector-projection = { path = "../vector-projection", version = "0.2.0" }
