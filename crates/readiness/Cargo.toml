[package]
name = "skein-readiness"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = "1"
serde_json = "1"
skein-core = { path = "../core" }
skein-cypher = { path = "../cypher" }
skein-evidence = { path = "../evidence" }
skein-executor = { path = "../executor" }
skein-plan-cache = { path = "../plan-cache" }
skein-route-ownership = { path = "../route-ownership" }
skein-storage = { path = "../storage" }

[dev-dependencies]
skein-search = { path = "../search" }
